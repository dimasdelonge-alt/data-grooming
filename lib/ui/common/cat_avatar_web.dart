import 'package:flutter/material.dart';

/// Web stub: always returns fallback since dart:io File is not available.
Widget buildFileImage({
  required String imagePath,
  required double size,
  required Widget fallback,
}) {
  return fallback;
}
